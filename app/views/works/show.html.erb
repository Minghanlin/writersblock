<% provide(:title, @work.title) %>
<p>
  <strong>Writer:</strong>
  <%= @work.user.name %>
</p>

<p>
  <strong>Title:</strong>
  <%= @work.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @work.content %>
</p>


<% if current_user === @work.user %>
<%= link_to 'Edit', edit_work_path(current_user) %>
<% end %>
